interface:
  display_name: "Markitdown"
  short_description: "Repo-specific patterns and workflows for markitdown"
  default_prompt: "Use the markitdown repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true